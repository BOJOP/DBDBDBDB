<% provide(:title, "Edit enrollment") %>
<h5>Update enrollment</h5>
<hr><br>
<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_for(@enrollment) do |f| %>
      <%= render 'form', enrollment: @enrollment %>
      <button type="submit" class="waves-effect waves-green btn" style="float:right;">Update</button>
    <% end %>
  </div>
</div>

<%= link_to 'Back', @enrollment.section %>
