<% provide(:title, "Edit rule") %>
<h5>Update rule</h5>
<hr><br>
<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_for(@rule) do |f| %>
      <%= render 'form', rule: @rule %>
      <button type="submit" class="waves-effect waves-green btn" style="float:right;">Update</button>
    <% end %>
  </div>
</div>

<%= link_to 'Back', rules_path %>
